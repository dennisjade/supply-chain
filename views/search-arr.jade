extends layout

block content
  .homepage-container.drawer.drawer--left
    include header
    include navigation

    #arr-page.container.container-table
      input(type="hidden", name="partNumber", value="#{partNumber}")
      input(type="hidden", name="classType", value="#{classType}")
      .row.vertical-center-row
        div(style='padding-top:70px')
          .row
            div.yesno-box.yesno-arr
              div.spinner-center
                span.glyphicon.glyphicon-refresh.glyphicon-spin
          .row
            div.arrTable(style="padding-top:10px;padding-bottom:67px;")
              div.spinner-center
                span.glyphicon.glyphicon-refresh.glyphicon-spin

    include footer